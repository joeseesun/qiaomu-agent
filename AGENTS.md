# 开发约定

## 现成组件优先

- 开发任何新功能前，尽可能先阅读 https://elements.ai-sdk.dev/components/，寻找匹配的现成组件及官方用法。
- 优先复用 AI Elements 源码与组合方式，再做最小 Obsidian 适配；不因为已有手写实现而跳过组件调查。
- 没有对应组件，或组件与宿主、移动端、可访问性、权限或性能要求冲突时，说明理由，再采用宿主原生组件或自行实现。
- 复用时记录来源版本、许可证及本地修改；示例 UI 不代表后端能力已经实现。
- 样式限制在插件范围，保留 Obsidian 主题、中文排版和原生笔记语义。

## 图标规范

- 用户长期偏好：今后开发界面默认优先使用 Lucide 图标库；React 项目使用 lucide-react，其他技术栈使用对应的 Lucide 集成。不要用 emoji 代替操作图标。
- AI 回复下方的操作栏仅显示 Lucide 图标，不显示常驻文字按钮：Copy（复制）、CalendarPlus（追加到今日日记）、FilePlus2（追加到指定文件）。
- 图标按钮必须保留可访问名称、可见键盘焦点和悬停/聚焦提示；提示文字不是常驻按钮文字。Obsidian 中沿用宿主可访问性与提示机制。
- 图标尺寸、描边和点击区域保持一致；不能为了纯图标外观省略写入目标确认、错误反馈或必要的安全提示。

## 产品界面

- 新组件先从第一次使用的用户视角确定任务、当前状态和下一步，再安排高级设置；默认不展示内部术语、冗余说明和长列表。
- 插件自有界面参考 Vercel 的中性色、清晰边界与克制的视觉层级；避免直接采用 Obsidian 的紫色强调控件。样式仅作用于插件范围，并检查浅色、深色、窄屏、键盘焦点及对比度。具体变量与层级见 `DESIGN.md`。
